Changelog for package kdemds

v 0.1 to 0.2-3
here be dragons...

v 0.2-4
plotKDE(): changed/fixed breaks: pretty_breaks() and trans_breaks() now get
the ... parameter passed, so the number of generated breaks can be adjusted by
passing the n=xxx parameter to plotKDE() directly. In logscale, breaks are now
generated by trans_breaks(), giving better and more breaks.